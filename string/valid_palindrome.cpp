#include<bits/stdc++.h>
using namespace std;
class Solution {
public:
    bool isPalindrome(string s) {
        if(s==" ")
            return true;
        string newstr="";
        for(auto x:s){
            if(isalnum(x)){
                if(	isalpha(x)){
                    
                }
            }
        }
    }
};